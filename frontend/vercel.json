{
    "version": 2,
    "builds": [
      {
        "src": "/",
        "use": "@vercel/next"
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/([locale]+)/(.*)", "dest": "/[locale]/$2" },
      { "src": "/(.*)", "dest": "/" }
    ]
  }
  