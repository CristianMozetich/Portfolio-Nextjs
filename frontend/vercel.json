{
    "version": 2,
    "builds": [
      {
        "src": "/",
        "use": "@vercel/next"
      }
    ],
    "routes": [
      { "src": "/frontend/src/app/([locale]+)/(.*)", "dest": "/[locale]/$2" },
      { "src": "/(.*)", "dest": "/" }
    ]
  }
  